<%= form_for @tag do |f| %>
	
	<%= render :partial => "/shared/visibility", locals: {f: f} %>
	
	<%= f.text_field :name %>
	<%= f.label :name %>

	<%= f.select :tag_type , Tag::TAG_TYPES.map{|c| [c,c]}, {include_blank: "Tag Type", disabled: '', selected: ''} %>

	<%= f.submit "Submit" %>

<% end %>