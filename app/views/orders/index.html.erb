 <table>
        <thead>
          <tr>
                <th>Order Name</th>
                <th>Reports</th>
                <th>Tubes</th>
                <th>Options</th>
            </tr>
        </thead>

        <tbody>
        	<% @orders.each do |order| %>
        	<% @order = order %>
        	<%= render :partial => "/orders/show" %>
        	<% end %>
        </tbody>
</table>
