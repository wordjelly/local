<h5>Rejected Employees</h5>
<ul class="collection">

	<% @organization.rejected_user_ids.each do |user_id| %>
			
		<li class="collection-item"><%= User.find(user_id).full_name %></li>

	<% end %>

</ul>
