<table>
        <thead>
          <tr>
              <th>Name</th>
          </tr>
        </thead>

        <tbody>
          <% @locations.each do |nr| %>
        	 <% @location = nr %>
           <%= render :partial => "show" %>
          <% end %>
        </tbody>
</table>
