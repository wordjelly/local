add new patient.
add tests to that patient (basically copy over the whole thing)
add packages
update the values of tests
add tests to existing tubes
lis connectivity
auto print.
lis update connectivity.
checklist approval by dr.sneha
report pdf generation, with signatures
abnormal value highlighting
total of test prices.

## so first of all it should get enqueued.

## after this reallot

## then routine scheduling.

## and then viewing the statuses

## and updating them.

## then UI for all of these.